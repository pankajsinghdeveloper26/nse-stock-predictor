# Leave unset to use Astro's dev-server proxy (see astro.config.mjs) and
# same-origin "/api/..." requests. Set this only if the frontend and the
# FastAPI backend are NOT served through the same proxy (e.g. static build
# deployed separately from the API), e.g.:
# PUBLIC_API_BASE_URL=http://localhost:8000
